---
description: Show Aevum temporal graph status for the active mission
argument-hint: "[mission-dir]"
---

Run trust-path status for WinAevum-Unify.

1. Resolve mission dir from `$ARGUMENTS` or `$AEVUM_MISSION` or `./mission`.
2. Execute: `unify graph status --mission <mission>`
3. Summarize authorizations and refuse to suggest `sh -c`.
