Print the Aevum AGENT_CARD — authorized capabilities, recent denials, how to act safely.

```bash
unify dream --mission "${AEVUM_MISSION:?set AEVUM_MISSION}"
```

Or via MCP: `aevum_agent_card`. Doctrine: authorize · attest · package · anti-slop (Inference only). Never `bypassPermissions`.
