---
description: Run offline AI-slop firewall and ingest as Inference only
argument-hint: "[mission-dir] [repo]"
---

Run `unify slop` (Trusted Autonomy ∩ anti-slop).

1. Mission = `$AEVUM_MISSION` or first arg; repo = second arg or `.`
2. `unify slop --mission <mission> --repo <repo> --all`
3. Remind: findings are Inference — they never authorize.
