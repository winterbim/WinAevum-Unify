---
description: Golden Path — side-branch, package, PR draft (never merge)
argument-hint: "[mission-dir] [repo]"
---

Run Golden Path under Aevum gates (`auto_merge=false`).

1. Mission / repo from args or env.
2. `unify golden --mission <mission> --repo <repo> --title "aevum golden"`
3. Never merge. Never suggest bypassPermissions.
